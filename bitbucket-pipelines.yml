image: thorgate/python3.4-node6

definitions:
  services:
    localstack:
      image: localstack/localstack
      environment:
        SERVICES: sqs,sns
pipelines:
  default:
    - step:
        script:
          # - pip install localstack
          - npm install
          - npm run test
        services:
          - localstack
        environment:
          LOAD_LOCALSTACK: false
  # custom:
  #   deploy-runtime:
  #     - step:
  #         script:
  #         - ./build-runtime.sh
  #         - scp -i ~/.ssh/config gm-runtime.zip ubuntu@52.27.128.130:~/tmp 

